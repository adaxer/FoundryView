<UserControl x:Class="FoundryView.Client.Wpf.Shell.Views.StatusBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FoundryView.Client.Wpf.Shell.Views"
             xmlns:viewModels="clr-namespace:FoundryView.Client.Wpf.Shell.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="800" d:DataContext="{Binding StatusBar, Source={StaticResource VmLoc}}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
<TextBlock Text="{Binding Message}"/>
<TextBlock Text="{Binding ApplicationTime}" Grid.Column="1"/>
</Grid>
</UserControl>
